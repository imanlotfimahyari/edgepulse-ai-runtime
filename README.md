# edgepulse-ai-runtime
A lightweight edge-AI runtime for industrial and IoT environments. It ingests telemetry from simulated edge devices over MQTT or HTTP, runs local anomaly/inference logic, exposes Prometheus metrics, and can be deployed with Docker or lightweight Kubernetes using Helm.
